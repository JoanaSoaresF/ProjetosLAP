{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc-8 build active file",
            "command": "/usr/bin/gcc-8",
            "args": [
                "-std=c11",
                "-g",
                "${fileDirname}/Cartography.c",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-lm"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}